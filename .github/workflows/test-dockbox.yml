---
name: Test — dockbox

on:
    push:
        paths: [dockbox/**]
    pull_request:
        branches: [master]

jobs:
    test:
        runs-on: ubuntu-latest
        timeout-minutes: 5
        steps:
            - uses: actions/checkout@v4
            - run: make -C dockbox test
            - run: bash tests/drift_test.sh
