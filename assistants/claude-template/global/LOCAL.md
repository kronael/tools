# LOCAL.md

Local paths and private configuration. Auto-injected by local.py hook.
NEVER checked into git. NEVER synced to template repos.

## Sync Paths

Sync ~/.claude/ changes back to the assistants template:

```
~/.claude/ => <path-to-assistants>/claude-template/global/
```
