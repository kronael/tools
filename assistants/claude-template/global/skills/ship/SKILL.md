name: ship
description: Plan and ship a feature. Explores codebase, writes specs/*.md, then runs ship CLI for autonomous execution. Use when asked to build something substantial.
user-invocable: true
