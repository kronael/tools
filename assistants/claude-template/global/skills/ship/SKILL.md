name: ship
description: Plan and ship a feature. Explores codebase, writes specs/*.md, then runs ship CLI for autonomous execution. Use when asked to build something substantial.
user-invocable: true

## Notes

- Design specs (specs/N/*.md) can be shipped directly — no
  need to rewrite them into implementation specs if they
  already contain concrete deliverables, file paths, and
  acceptance criteria
- Sync this skill across assistants repos (see LOCAL.md)
