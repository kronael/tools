---
name: oracle
description: Alias for /codex — ask the codex CLI for a second opinion. NOT for routine lookups (use grep/read/recall-memories). NOT a Claude Agent — this is the OpenAI codex CLI.
when_to_use: "oracle, second opinion, sanity check, ask codex, disagreement after reasoning"
user-invocable: true
---

# Oracle

Alias for **/codex**. The full runbook lives in `skills/codex/SKILL.md`.

Load that skill and follow it — auth check, `-s danger-full-access`,
adversarial framing, model-pinning rules all live there.
