---
name: q
description: /q — question mode, read and explore only. NOT for making changes (use fin or just ask).
when_to_use: "just answer", "don't touch anything", "explain this", "read only", "question mode"
user-invocable: true
---

# /q — question mode

Answer and explore. NEVER write, edit, or create files.

## Behavior

- Read files, run Bash for exploration, search, grep — all fine
- NEVER use Edit, Write, or NotebookEdit tools
- If the answer requires a change, describe it instead of making it
