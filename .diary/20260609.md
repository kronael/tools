---
summary: |
  tools repo — dockbox sandbox + skills bundle.
  - dockbox auto-resumes last session; no past session → starts fresh
  - /tmp in container now tmpfs
  - libssl-dev added to image for openssl-sys crates
---

## session

dockbox: made `--resume` the default by auto-detecting `~/.claude/projects/<slug>/*.jsonl`. Added `-N` to skip, then replaced it entirely with auto-detection — cleaner. Released v0.3.9 (resume default) and v0.3.10 (-N flag, short-lived).

dockbox Dockerfile: added `libssl-dev` (openssl-sys needs dev headers, not just runtime lib). Mounted `/tmp` as tmpfs — `/tmp/cargo-target` nested on top still works.

fix skill: auto-loads `./capture.png` or `/tmp/capture.png` when bug target is unclear.

CLAUDE.md synced to global development wisdom. Skills updated: go/py/rs naming conventions aligned, oracle skill fixed codex stdin block (`</dev/null` required), commit skill clarified orphaned worktree scope.
