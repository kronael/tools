---
summary: |
  tools repo — dockbox sandbox + skills bundle.
  - dockbox image broken: playwright chromium install fails on Debian forky [resolved 2026-06-10: explicit chromium deps, dropped --with-deps]
  - release pending: 12 commits since v0.3.10 [resolved 2026-06-10: tagged v0.3.11]
  - v0.3.12 released 2026-06-10: fable skill, xhigh/high effort levels, libfontconfig1 fix
---

## session

dockbox Dockerfile: two build fixes. Added `libpq-dev` for pg_config / postgres crates. Fixed playwright chromium install — Debian forky t64 transition renamed `libfreetype6` → `libfreetype6t64` etc. and some font packages don't exist; dropped `--with-deps`, install chromium deps explicitly with forky-correct names.

Release pending from previous session invocation — not yet tagged.

## 17:56

Released v0.3.11. 13 commits since v0.3.10: dockbox auto-detects past session (checks ~/.claude/projects/<slug>/*.jsonl, drops -N flag), /tmp as tmpfs, libpq-dev+libssl-dev in image, playwright forky fix, oracle stdin unblock. Tagged and CHANGELOG written.

## 18:32

Released v0.3.12. Added /fable skill (claude-fable-5, xhigh effort). Updated /opus to xhigh effort, /sonnet to high effort. Synced opus/sonnet/fable/haiku skills to tools repo. Also included post-v0.3.11 fontconfig fix (libfontconfig1t64 → libfontconfig1).

## 19:40

Said "install" — ran the kronael install procedure into ~/.claude. Backed up to ~/.claude/backup/20260525-080935. Tightened settings.json: added gh-push deny rules, CLAUDE_CODE_DISABLE_AUTO_MEMORY env, sandbox.enabled=true. Verified dockbox/dockbox:167-170 overmounts settings.local.json inside the container with bypassPermissions+sandbox.enabled=false — so host can stay strict while dockbox stays unrestricted. Cherry-picked the gh-push NEVER rule into ~/.claude/CLAUDE.md; updated source skills/global/SKILL.md to clarify .diary is "generally public unless project marks it local-only" (was "checked into git, long-lived"). agent-browser 0.27.0 installed.

Ported 16 creative skills from NousResearch/hermes-agent (skills/creative/) into skills/. Dropped 4 that need external services (baoyu image-gen API, comfyui cloud/GPU, songwriting-and-ai-music → Suno, touchdesigner-mcp). Renamed remaining 12 to `create-*` prefix to group them and avoid collisions with engineering skills; patched the `name:` field in each frontmatter. Documented the convention in README.md, CLAUDE.md (### Skill naming under ## Agents and Skills), and skills/README.md (new create-* generators table). Committed as 5879bf9.

Merged origin/master. Two conflicts:
- CLAUDE.md: kept origin's "Code deletion lowers costs" paragraph in the philosophy list; moved HEAD's Skill naming subsection to its proper home under "## Agents and Skills" rather than the middle of Boring Code Philosophy.
- skills/go/SKILL.md: took origin's naming rules (align with project's "Short variable names OK: n,k,r,i…" policy added to CLAUDE.md) plus kept HEAD's package-name guidance about underscores.

Cleanup as part of the merge: removed skills/oracle (codex CLI second-opinion, unused) and skills/opus (standardize on fable). Updated skills/sonnet escalation arrow to point at /fable; rewrote skills/fable's "prefer /opus" footer to "prefer /sonnet or /haiku". Merge committed as c903a08.

Wrote specs/5-web-onepager.md (244 lines). Plan: turn README into a dense, terminal-native single-scroll landing — eval-style above the fold (hero asciicast, install, skill catalog, before/after), contributor-style below (architecture, hook lifecycle, dockbox). 5 anchor visuals: hero asciicast (.cast + static SVG), bundle architecture SVG pair (light/dark via `<picture>`), install+hook lifecycle ASCII, skill catalog grid (ASCII boxes), dockbox sandbox SVG pair. 9 implementation phases, each independently mergeable. No separate site — GitHub renders it. Assets in `assets/`. Open questions called out (asciicast hosting, light/dark vs neutral palette, catalog freshness via generator script, README length cap, `docs/` vs `assets/`).

## 22:30

Hooks audit — earlier "the nudges are a mess" was a stale mental model. Actual state is complete + consistent: 6 hooks wired identically in ~/.claude and repo settings.json. prompt_nudge.py (old keyword/commit/docs nudge) is restored, not gone. post_tool_nudge.sh (PostToolUse, throttled 100 calls/10min) delegates to stop.py for commit+diary nudge — diary already fires from post-tool path. CLAUDE.md stale nudge.py reference already removed. Open: 3 untracked files (hooks/post_tool_nudge.sh, hooks/prompt_nudge.py, settings.json) not yet committed.
