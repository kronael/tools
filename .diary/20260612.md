---
summary: |
  kronael tools repo. Maintaining Claude Code bundle (skills/hooks/agents).
  - skills/ops/SKILL.md has stale committed conflict markers (pre-existing)
  - ascfix, diagram skill, WORKFLOW.md all shipped yesterday (5d7618b)
---

## session

Folded skills/WORKFLOW.md into skills/README.md (it was just a readme). Diagram now inline under "## Skill workflow diagram". Deleted the separate file.

## later

Ran CTO + CEO evals (fable, codex broken on gpt-5.5). CTO: TRIAL — flagged no CI, ascfix missing from TEST_DIRS, hook stdin-at-import defect, doc drift. CEO: ADOPT WITH CONDITIONS — bus factor 1, pin a release tag, review push/squash bans.

Fixed root Makefile: replaced TEST_DIRS/CLEAN_DIRS with single PROJECTS var using morha auto-generated pattern. `make workflows` scans for test+clean intersection. ascfix now wired in. `make test` passes both projects.

## cto fixes + udfix

Knocked out CTO quick-wins: guarded all 4 hooks' main() behind `if __name__`, fixed README hook names (prompt_nudge/pretool_nudge), added NOTICE acknowledging all upstream (humanizer=blader, hermes-agent, design.md, get-shit-done). New skills/credits/SKILL.md: attribution practice for LLM-vibed work (pattern from arizuko NOTICE).

Renamed ascfix → udfix (unicode-diagram-fix). Tried a PostToolUse hook to auto-fix diagrams, then dropped it — auto-fixing belongs at pre-commit/general-install layer, not Claude-only. udfix stays a tool; skills/diagrams (renamed from diagram, plural like bugs/specs) is its documented home. @readme agent told to pipe ARCHITECTURE.md diagrams through udfix.

Open: twitter_dump.py untracked in repo root (not mine). ship-into-tools deferred (own repo/release cycle).

## docs refine

Updated codex CLI 0.120→0.139 (bun add -g @openai/codex@latest) — old version couldn't use gpt-5.5. Codex audited docs critically: found duplication + drift. Verified every claim before acting.

Killed WORKFLOW.md — it described a /ship→/build→/refine hierarchy but /build skill never existed (pure fiction). Real workflow lives in skills/README.md cluster diagram. Dropped dead 'build':'/build' route from prompt_nudge.

Two fable agents (non-overlapping doc sets) rewrote for single-owner facts: README owns CLI inventory + docs map, ARCHITECTURE owns install rationale (was triplicated), settings/hook-source own wiring. skills/README replaced drift-prone per-skill tables (missing credits/hacker-eval/video-*) with categories + `ls skills/` pointer. hooks/ARCHITECTURE: fixed reclaude.py event (PreCompact-only, docs said UserPromptSubmit). Removed empty skills/code-review dir. Fixed /dispatch→/resolve in wisdom, SessionEnd→PostToolUse in install SKILL. All docs now well under line budgets.

Also recorded CI single-source-of-truth pattern in ~/wk/mnde/rust-template (SUGGESTIONS + modernized code-quality.yml: actions-rs→dtolnay pinned, derive toolchain from rust-toolchain.toml, checkout-first). Left uncommitted (separate repo).

## release v0.3.16

Pre-release eval gate: opus CTO + hacker-eval in parallel. Both found real blockers (verified, not trusted):
- CTO: udfix/Makefile still built `ascfix`; two binaries committed to git. Both fixed.
- hacker MED: hook state in shared /tmp (symlink-clobber, + own NEVER-/tmp rule) → ~/.claude/tmp; trufflehog curl|sh → go install. hacker LOW: stop.py made .diary in any cwd → gated on git repo.
- Non-blockers deferred: no .github CI (low weight for config bundle), local.py LOCAL.md injection (by design), untracked twitter_dump.py.
Closed all blockers, re-verified (tests pass, hooks smoke-tested), tagged v0.3.16. Not pushed.
